services:
  app:
    build:
      context: .
      dockerfile: dev.Dockerfile
    volumes:
      - ./:/usr/src/app:cached
      - /usr/src/app/node_modules
    ports:
      - 5173:5173
      - 24678:24678
    environment:
      CHOKIDAR_USEPOLLING: "true"